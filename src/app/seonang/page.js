import { basicMeta, basicViewport } from '../basicmeta';
import Seonang from '@/components/seonang/seonang';

export const metadata = basicMeta;
export const viewport = basicViewport;

export default function SeonangPage() {
    return (
        <Seonang />
    )
}